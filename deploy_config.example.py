# Ish-khwaz deploy credentials — template. Copy to deploy_config.py and
# fill in real values; deploy_config.py itself is git-ignored.
HOST = "your.server.ip.or.host"
PORT = 22
USER = "your-ssh-username"
PASS = "your-ssh-password"
REMOTE_PATH = "/path/to/your/deployment/directory"
